# Dummy secrets for building/validating the example configurations.
#
# These are throwaway placeholder values so the examples can be built and
# validated (e.g. in CI) out of the box. Replace them with your own values
# when flashing a real device.

# Wi-Fi credentials
wifi_ssid: "example-ssid"
wifi_password: "example-password"
wifi_fallback_password: "example-fallback"

# Home Assistant API encryption key (throwaway, valid base64 32-byte key).
# Generate your own with: openssl rand -base64 32
api_encryption_key: "JlaSlSYtL3SdDxigjwCTo5kuvafwsbZ2USE0BxEQ4zc="
